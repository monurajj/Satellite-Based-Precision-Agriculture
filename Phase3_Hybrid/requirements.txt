# Phase 3 Hybrid System Python dependencies
# Install: pip install -r requirements.txt
# macOS extra: brew install libomp   (required by XGBoost)

# Core ML
xgboost>=2.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# DL inference (Phase 2 model loading)
torch>=2.0.0
torchvision>=0.15.0
timm>=0.9.0
Pillow>=10.0.0

# API clients
requests>=2.31.0

# Notebook execution + plots
jupyter>=1.0.0
nbconvert>=7.0.0
matplotlib>=3.7.0

# Optional: for paired CNN feature extraction with Grad-CAM (already in Phase 2 deps)
# grad-cam>=1.4.0
